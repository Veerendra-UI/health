part of 'pin_screen_generate_bloc.dart';

class PinGenerateScreenEvent {
  var pin;
  var generate;
  var memberId;

  PinGenerateScreenEvent(this.pin, this.generate, this.memberId);
}

///this is event class for pin generation.
