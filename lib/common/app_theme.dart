import 'package:flutter/material.dart';

import 'app_colors.dart';

class AppTheme {
  static ThemeData get() {
    return ThemeData(
        primaryColor: CustomizedColors.primaryColor,
        accentColor: CustomizedColors.accentColor);
  }
}
